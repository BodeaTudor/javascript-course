/*
* the difference between null and undefined is that null is an intentional lack of value and undefined isn't
*/

let age;
console.log(age, age + 3, `the age is ${age}`);

let year = null;
console.log(year, year + 3, `the year is ${year}`);